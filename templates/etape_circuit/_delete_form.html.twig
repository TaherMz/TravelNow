<form method="post" action="{{ path('etape_circuit_delete', {'id': etape_circuit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etape_circuit.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
